{
  "id": "doom-emacs",
  "version": "0.0.2",
  "name": "Doom Emacs",
  "description": "Doom Emacs configuration",
  "options": {
    "version": {
      "default": "latest",
      "description": "Select the version to install.",
      "proposals": ["latest"],
      "type": "string"
    },
     "sync_config_from_git_web_url": {
      "default": "latest",
      "description": "Get a Doom Emacs config files from a git web url.",
      "type": "string"
    }
  },
  "installsAfter": ["ghcr.io/devcontainers-contrib/features/homebrew-package"]
}
